# ai-input-method
A Chrome extension that provides an input method for emojis, special characters, and foreign language input, via the SidePanel UI. The side panel includes a searchable list of emojis and symbols, allowing users to easily insert them into text fields.
